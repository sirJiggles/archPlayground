{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel",
    "dev": "lerna run start",
    "deploy": "lerna run up"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
